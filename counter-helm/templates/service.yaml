apiVersion: v1
kind: Service
metadata:
  name: {{ include "counter-helm.fullname" . }}
  labels:
    {{- include "counter-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "counter-helm.selectorLabels" . | nindent 4 }}
